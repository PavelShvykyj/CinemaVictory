export enum TicketOperations {
    Sale,
    Reserve,
    Cansel,
    SaleReserve,
    Nothing,
    CanselPay
  }

  export enum HallShowStatus {
    Defoult,
    Reserving,
    Cancel,
    StartSale,
    Search
  }  

  export enum MessageSate {
    Error,
    Info,
    Sucsess
  }
