export enum TicketOperations {
    Sale,
    Reserve,
    Cansel,
    SaleReserve
  }

  export enum HallShowStatus {
    Defoult,
    Reserving,
    Cancel,
    StartSale,
    Search
  }  

  export enum MessageSate {
    Error,
    Info,
    Sucsess
  }
